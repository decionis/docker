{
  "name": "decionis-mcp-image",
  "private": true,
  "description": "Pin manifest for the decionis/mcp container image. Single dependency: the published @decionis/mcp artifact this image wraps, at an exact version (rules/security.rules.md Rule 1.3); package-lock.json carries the integrity hash.",
  "license": "Apache-2.0",
  "dependencies": {
    "@decionis/mcp": "0.1.2"
  }
}
